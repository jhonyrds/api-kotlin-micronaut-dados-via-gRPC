micronaut:
  application:
    name: fretes

grpc:
  server:
    health:
      enable: true

endpoints:
  all:
    port: 8085
    sensitive: true
  health:
    enable: true
    sensitive: false
    details-visible: ANONYMOUS
